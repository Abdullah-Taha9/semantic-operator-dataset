[project]
name = "semantic-operator-dataset"
version = "0.1.0"
description = "Dataset labeling for semantic operators"
requires-python = ">=3.12,<3.13"
dependencies = [
    "vllm==0.25.1; sys_platform == 'linux'",
    "litellm",
    "pyarrow",
    "huggingface_hub",
    "python-dotenv",
]

[tool.uv]
package = false